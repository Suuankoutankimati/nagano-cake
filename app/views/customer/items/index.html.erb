<h1>商品一覧</h1>
<h3>全<%= @items.count %>件</h3>

  <% @items.each do |item| %>
    <div>
      <%= link_to item_path(item) do %>
    		<%= attachment_image_tag item, :image %>
    	<% end %>
  		<%= item.name %>
  		¥<%= item.non_tax_price %>
		</div>
	<% end %>
<%= paginate @items %>